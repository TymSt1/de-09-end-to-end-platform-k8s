-- Fact table: all enriched rides
select
    ride_id,
    event_ts,
    event_date,
    event_hour,
    pickup_zone_id,
    pickup_zone_name,
    pickup_borough,
    dropoff_zone_id,
    dropoff_zone_name,
    dropoff_borough,
    passenger_count,
    trip_distance_miles,
    duration_minutes,
    payment_type,
    rate_code,
    fare_amount,
    tip_amount,
    tolls_amount,
    total_amount,
    fare_per_mile,
    fare_per_minute,
    tip_percentage,
    is_rush_hour,
    is_weekend,
    distance_category,
    fare_category,
    tip_category,
    borough_flow
from {{ ref('int_rides_enriched') }}